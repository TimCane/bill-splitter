# Sidecar runtime. PaddleOCR/PaddlePaddle wheels are added here in M3 when real
# inference is wired (docs/06-ocr-service.md); M1 runs the stub only.
fastapi==0.121.2
uvicorn[standard]==0.40.0
pillow==11.3.0
pydantic==2.12.4
